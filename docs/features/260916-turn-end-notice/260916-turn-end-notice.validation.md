# 260916-turn-end-notice 用户验证

## 验证说明

- 验证对象：turn 正常收尾时在轮间注入 `Turn ended: <时间戳>` 关闭读数，下一轮模型可见上一轮结束时间
- 环境/前置条件：线上 dsh（4175）已完成本次插件更新并重启（重启需用户书面同意，另行确认）

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| -------- | -------- | -------- | ---- | --------- |
| 完成一个正常对话轮（模型自然结束回复），在 GUI 消息流中查看该轮尾部 | 出现折叠的 `Turn ended: <星期 日期 时间 +08:00>` context chip，时间与轮次结束时刻吻合（误差秒级内） | | 待验证 | |
| 开始下一个新对话轮，询问模型"上一轮是什么时候结束的" | 模型能直接读出上一轮结束时间，与 GUI 中关闭读数一致 | | 待验证 | |
| 中途手动停止一个正在运行的轮次 | 不出现关闭读数（中断轮无 `Turn ended` 通知），后续对话正常不受影响 | | 待验证 | |
| 观察GUI 折叠行与注入稳定性：连续 2-3 个正常轮次 | 每轮恰一条 `Turn ended` 通知；无报错、无 turn 异常中断、`sv status dsh` 正常 | | 待验证 | |

## 验证结论

待验证。

## 待跟进

无。
